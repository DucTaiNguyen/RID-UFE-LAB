name: Build PDF

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install LaTeX
        run: sudo apt-get install -y texlive-full

      - name: Build PDF
        run: |
          pdflatex report.tex
